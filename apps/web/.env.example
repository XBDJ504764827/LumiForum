# Optional app-local overrides for Next.js only.
# Prefer root .env for docker compose; copy these when running `pnpm dev:web` alone.

NEXT_PUBLIC_API_URL=http://192.168.0.138:8080
API_INTERNAL_URL=http://localhost:8080
